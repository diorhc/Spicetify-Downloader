// app.js -- intentionally empty.
// The custom-app entry point is index.js (loaded by Spicetify automatically).
// The background extension is downloader.js (listed under subfiles_extension in manifest.json).

