// settings.js — runs first (before app.js) when the custom app page opens.
// Shared constants available to all subfiles and the extension.
const DOWNLOADER_VERSION = "1.0.0";
console.info(`[Spicetify Downloader] v${DOWNLOADER_VERSION} page loaded`);
